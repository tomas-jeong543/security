정적 분석 : 코드 실행 안하고 코드 분석하는 것
동적 분석: 실제 실행하면서 결과를 확인하는 것
좋은 경우 매우 난해할 때 직접 실행해보면서 확인하는 게 더 빠르다
but computer에서는 중단점을 설정에서 특정 시점의 상태를 볼 수 있다. 그걸로 시점별로 아 어느 시점에 어느 지점에 실행이 되는 구나
로 관련 정보를 연관지을 수 있다는 장점이 있어서 매우 중요하다. 문맥속 관계를 파악 가능하다

중단점을 거는 게 중요한데 그걸 정하는 게 정적 분석이다. / 이걸 해서 너무 복잡하면 동적 분석을 한다

결국 정적 -> 동적 -> 정적 -> 동적

x64 화면 설명

왼쪽 위-> 메모리의 디스어셈블리 뷰, 왼쪽 아래: 메모리 덤프, 오른쪽  아래: 스택 메모리를 보여준다 rsp부터 쭉 보여주는 view다 
현재 실행하는 지역변수, 오른쪽 중간: 인자를 순서대로 알려준다, 오른쪽 위: cpu 레지스터 상태

프로그램 실행 상태 = 프로세스 -> 프로세스별로 가상 주소를 사용함 ->실제론 다른 물리
메모리에 저장이 됨
메모리 덤프는 가상 메모리 주소를 보여준다 -> timer.exe 실행 파일이 메모리에 올라와 있는 거다

매모리에는 어셈블리어가 아닌 기계어가 들어있고 왼쪽 위는 그걸 어셈블리어로
해석해서 보여주는 거다.

모든 화면은 동일한 값을 다양한 방식으로 보여준다.

동적분석 순서

1.진입점 찾기(보고 싶은 점 주소를 찾기)
f9눌러 실행한 이유는 모듈이 실행파일이 되게 해서 우리가 원하는 실행파일에서 원하는 문자열을 찾으려고 한 거다.
주석 세미콜론, f2 중단점 설정

f7, step into -> 함수 안으로 들어감
f8 step over -> 함수 실행 이후로 감

덤프에서 따라가기 하면 문자열 있는 곳으로 간다
ida에서 buffer부분이다.

프로그램의 값이 가상 메모리에 매핑되는 걸 알 수 있다.

우클릭 후 메모리에서 따라가기로 정적과 동적 분석의
매핑을 할 수 있다

디스크에 저장된 exe파일은 MZ부터 시작해서 cannot berunned in ~~~ 로 저장되어 있는데 그게 그대로 프로세스로 올라간다(약간은 alignement가 달라질 수 있다)

프로세스 1000바이트 단위로 하고 빈 부분은 패딩으로 채운다 프로세스와 프로그램의 차이점이다.

프로세스 베이스 맞추기 ida <-> x64dbg
1. x64dbg 메모리 주소 복사
2.ida - edit - segments - 복사한 주소를 16진수로 바꿔서 변경해 저장한다.
프로그램과 프로세스를 맞춰서 그걸 이용해서
분석하고 쉬운 함수로 찾을 수 있다. 분석이 
엄청 쉬워지기 때문에 매우 중요한 방법이다.
ida -g, x64 ctrl + z or g 로 주소 복사시 이동 가능

f8을 눌러서 디버깅하면서 폭탄을 눌르면서 관계성을
파악할 수 있다.

sleep 숫자를 작게 바꿀 수 있다 

patch하는 법 hxd를 이용해 exe파일의 복사본을 만들고 그 다음에 ctrl + g로 원하는 위치에 가서 값을 바꾸고 실행하면 영구적으로
바꾼다 



중단점 설정하고 재시작하고 f9 3번 누르면 중단점 설정한 곳으로 간다


timer2.exe sleep함수 치고 거기 중단점 설정하고 실행 후 거기로 가서 0으로 하고 실행하면 그걸 엄청 많이 반복해야 함
분석시 edi엔 ecx가 들어감 그리고 그 후 ecx는 쓰이지 않음 delay는 ecx에 정해짐 ecx -> 0 를 xor edi edi로 바꾼다 이건 못해서
한 번 해보기 자동화하는 방법

api함수 우회해서 하는 법
코드가 복잡해질수록 중요해진다


 
