ida와 xdbg64주소를 동일하게 맞춘 뒤에 이를 이용해서 ida의 데이터 주소를 통해 특정값을 xdbg에서 동적으로
추적하는 방식에 대해 알아보자
예를 들어서 ida에서 data주소가 00007FF676F6A000라고 할때 xdbg에서 Ctrl + G → 00007FF676F6A000로 입력하고 그 값을 확인
한 다음에  우클릭 → Breakpoint 를 한 다음에
 읽는 순간을 잡고 싶으면: Hardware on access / on read ,
  값이 바뀌는 순간을 잡고 싶으면:
Hardware on write를 
특히 암호화된 문자열이면 보통:암호화된 data 주소 → on access, 
복호화 결과 버퍼 → on write를 한 다음에
7. F9 실행
8. 멈춘 위치가 그 data를 사용하는 코드다.

동적 분석하다가 느낀 건데 그냥 이름 하나 입력하고 정답 serial번호를 가리키는 레지스터 값을 덤프에서 따라가기해서 로그인 하면 중간 과정의 결과가 전부 필요없어지는 데 이런 접근 방식이 더 난이도 높은 문제에서 어떤 한계나 약점이 될 수 있나?에 대한 핵심 결론

현재 방식				어려운 문제에서 막히는 이유
정답 serial 레지스터 따라가기	정답이 평문으로 안 만들어짐
덤프에서 문자열 보기		내부 값이 바이너리/암호문/해시일 수 있음
비교 직전 값 확인			비교 함수가 직접 구현되거나 숨겨짐
한 번 실행해서 답 얻기		안티디버깅/타이밍/가짜 경로 가능
정답 문자열 복사			서버 검증이면 로컬에 정답 없음
특정 name의 serial 얻기		keygen 제작에는 알고리즘 이해가 필요
이러한 이유 때문에 동적분석을 할 때에도 중간 과정을 하나하나 검증해야 한다. 

어떤 함수의 반환값을 동적분석에서 확인하는 방법은 RAX/EAX/AL를 확인하는 것이다.


