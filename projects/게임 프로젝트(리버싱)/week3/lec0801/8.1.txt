8.8일날 수업은 없다.

다다음주까지 8.15일까지 게임을 가지고 리버싱용으로 자세히 분석을 해보면 된다.

주제 = 패커 or 프로텍터 -> 정체가 뭔지 한 번 자세히 찾아보기 upx패커의 동작 원리 ida/ xdbg64를 알아보기,
+ 패커를 어떻게 만들 수 있을까? 패커를 어떻게 해제할 수 있을까? 패커를 우회를 할 수 있을까?

안티디버깅, 난독화 어떻게 분석을 할 수 있을까? -> client 단위에서의 방어


서버 입장에서는 어떻게 방어를 할 수 있을 것인가?

d를 누르면 바이트를 ida에서 구분을 할 수 있다 - > 구조체 기능으로 만들 수 있다/ c문법으로 만들 수 있다.

양식 없이 작성을 해도 괞찮다.


분석에서 배운 점

penguinrabit.exe

디버거 빨간점 분석해 보기 680줄

exe파일 -> dat파일 호출하는 위치 찾기 


벽돌깨기 게임 -> 패치를 함 update함수에 speed_mult가 곱해지는 데 그걸 늘리면 속도를 늘릴 수 있다.
복사본을 만들고 hex editor로 그 위치로 가서 그걸 바꾸면 된다 

패치하는 걸 한다 -> 이걸 막을려면 서버를 구축하고 그걸 서버에서 주로 검증하고 보안을 강화하는 게 좋겠다.


///

패킹된 파일과 패킹이 안 된 파일의 비교를 해보자

함수 텝에 함수가 안 보인다.

패킹을 해도 똑같이 실행은 된다 다만 분석이 더 어렵게 만든 것 뿐이다.

패킹을 해도 언패킹을 해야 결국 실행이 제대로 된다는 점이 중요하다.

실행 후 디버거를 붙여보면 언패킹이 되어 있다. 다만 section부분이 약간 비틀려 있다/

하지만 처음 디버거로 실행하면 깨진 상태로 존재한다. -> 보다보면 어디가 마지막 부분인지를 알 수 있다

아주 자세하고 디테일하게 조사

pe파일 구조를 조사하면 좋을 듯 하다






