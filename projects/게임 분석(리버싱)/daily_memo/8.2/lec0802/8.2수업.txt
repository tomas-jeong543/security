목표: 벽 렌더링? 구현

해야할 것

1. 벽 (사각형) 좌표가 어떤식으로 저장되었는지를 확인

클라이언트단에 저장
서버에서 벽 정보를 뿌려줌

가설 -> 저 부분이 벽 렌더링하는 부분일 것이다.
-> 매 순간 벽 렌더링 가정

1.저 부분은 벽 렌더링 부분이 아니다
2. 벽은 처음에만 초기화를 한다.

재시작시 저기 부분이 안걸려서 저 부분이 렌더링 부분이 아니라는 걸 알 수 있다.
만약 처음에 걸렸으면 저 부분에서 처음만 초기화를 하는 것이였을 것이다.

55,58,70 인 부분 네 군대를 전부 중단점을 걸어서 확인 -> 벽이 아님 확실히

한 군데가 걸렸음 색깔을 바꿨더니 배경색이었답니다 ㅎㅎㅎ

95,100,115 -> 여기가 벽 맞다


ida type을 구조체를 정의할 일이 생긴다. 이런 걸 복원할 필요가 있다  shift + f1누르면 type -> add type -> c language syntaxt로 타입 선언 가능


서버로부터 g_walls 배열에 벽 데이터를 x,y,width,height를 2바이트씩 4개를 총 8바이트의 정보를 받는다.

sdl rect -> 16바이트 관리

server client 는 2바이트 * 4 = 8바이트로 관리를 한다

다음주는 수업이 x ->
