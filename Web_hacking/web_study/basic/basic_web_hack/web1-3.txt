웹 개발 시 프론트의 자바스크립트나 백엔드 api에서 버그가 발생하는 경우가 잦고, 적용한 
css가 맘에 들지 않을 때도 많다 -> 이런 경우에는 개발자 도구를 이용해서 변경을 보통 하는데 이는 웹 취약점을 이용하려는 공격자에게도 유용하게 사용이 될 수 있다. 

개발자 도구를 실행하려면 브라우저를 열고 f12를 누르면 간단한 레이아웃 정보를 확인 가능하다.
화면의 상단을 보면 왼쪽부터 다음과 같은 기능을 수행한다.
🔴 빨간색
요소 검사(Inspect) 및 디바이스 툴바(Device Toolbar)

🟠 주황색
기능을 선택하는 패널. 강의에서는 굵게 처리된 기능들만을 다룹니다.
• Elements: 페이지를 구성하는 HTML 검사
• Console: 자바 스크립트를 실행하고 결과를 확인할 수 있음
• Sources: HTML, CSS, JS 등 페이지를 구성하는 리소스를 확인하고 디버깅할 수 있음
• Network: 서버와 오가는 데이터를 확인할 수 있음
• Performance
• Memory
• Application: 쿠키를 포함하여 웹 애플리케이션과 관련된 데이터를 확인할 수 있음
• Security
• Lighthouse

🟡 노란색
현재 페이지에서 발생한 에러 및 경고 메시지

🟢 녹색
개발자 도구 설정

이 중 요소 검사를 활용하는 경우엔 특정 요소의 개괄적인 정보를 파악하고 이와 관련된 코드를 쉽게 찾을 수 있다. 그리고 클릭하면 그 부분에 대한 html코드를 볼 수 있다.

그 다음에는 디바이스 쿨바를 활용하면 현재 브라우저의 화면 비율과 user_agent를 원하는 
값으로 변경할 수 있다 -> 이는 자신이 개발한 웹 서비스가 다른 장치에서도 잘 동작하는지
점검할 때 사용을 할 수 있다.

Elments영역으로 가면 현재 페이지를 구성하는 HTML의 코드를 읽을 수 있으며 코드를 선택한 상태로 단축키 f2를 누르거나 더블 클릭시 이를 수정할 수 있다. 이를 요소 검사 기능을 같이 활용하면 수정할 코드를 빠르게 선택할 수 있어서 편리함

콘솔은 프론트엔드 js 코드에서 발생한 각종 메시지를 출력하고 이용자가 입력한 자바스크립트 코드를 실행도 해주는 도구이다. 자바스크립트로 웹 개발을 할 때 console 오브젝트에는 개발자 도구의 콘솔에 접근할 수 있는 함수가 정의되어 있다. 따라서 코드를 작성하면서 어떤 변수의 값을 중간에 출력해보고 싶으면 console.log등을 유용하게 사용 가능하다.

예시
> console
Console {
  log: [Function: bound consoleCall],
  debug: [Function: bound consoleCall],
  info: [Function: bound consoleCall],
  dirxml: [Function: bound consoleCall],
  warn: [Function: bound consoleCall],
  error: [Function: bound consoleCall],
  ...
  context: [Function: context],
  [Symbol(counts)]: Map {},
  [Symbol(kColorMode)]: 'auto' }

단축키 ctrl + shift + j로 콘솔로 브라우저에서 js를 실행하고 그 결과를 확인할 수 있다

// "hello" 문자열을 출력하는 alert 함수를 실행합니다.
alert("hello");
// prompt는 popup box로 이용자 입력을 받는 함수입니다.
// 이용자가 입력한 데이터는 return value로 설정됩니다.
var value = prompt('input')
// confirm 는 확인/취소(yes/no)를 확인하는 이용자로부터 입력 받는 함수입니다.
// 이용자의 선택에 따라 Boolean(true/false)타입의 return value를 가집니다.
var true_false = confirm('yes or no ?');
// document.body를 변경합니다.
document.body.innerHTML = '<h1>Refresh!</h1>';
// document.body에 새로운 html 코드를 추가합니다.
document.body.innerHTML += '<h1>HI!</h1>';

애를 한 번 실행해 봐라


sources에는 현재 페이지를 구성하는 웹 리소스등을 확인할 수 있다.

왼쪽부터 현재 페이지의 리소스 파일 트리,파일 시스템 , 선택한 리소스 상세 보기, 디버깅 정보가 있고 디버깅 정보에 대해서 좀 더 자세히 분석을 해 보면 call stack은 함수들의 호출 순서를 스택 형태로 보여주며 a->b->c순서로 함수가 호출되서 현재 c코드 내부를 실행하고 있다면 call stack의 가장 위에는 c 가장 아래에는 a가 위치한다.
scope로 정의된 모든 변수들의 값을 확인할 수 있다
breakpoints로 브레이크포인트들을 확인하고 각각을 활성화실 또는 비활성화할 수 o

실제로 드림핵에서 제공한 예시 웹사이트에서 로그인을 하는 웹페이지에서 id,pw값을 입력하고 검증하는 부분에서 js파일을 보고 scope를 통해서 올바른 값으로 바꾸는 실습을 해 보았다.

그 다음에 네크워크 부분에서는 왼쪽 상단 바로 아래에 로깅 중지 및 로그 전체 삭제 기능이
그 아래에는 로그 필터링 및 검색 기능, 그 옆엔 옵션 기능이 존재한다.
옵션 기능에는 preserve log -> 새로운 페이지로 이동해도 로그를 삭제하지 않는 기능과
disable cache: 이미 캐시된 리소스도 서버에 요청하는 옵션이 존재한다.

그 다음 그 아래의 중앙 전체에는 네트워크 로그가 존재하고
그 아래에는 네트워크 로그 요약정보가 존재한다.
네트워크 로그에서 원하는 항목을 선택하면 해당 요청 및 응답 데이터를 확인할 수 있다.
또한 이러한 로그를 우클릭하고 copy에서 원하는 형태로 복사할 수도 있다.
만약 copy as fetch로 http request를 복사하고 console패널에 붙여서 실행하면 동일한 요청을 서버에 재전송할 수 있다.

최신 브라우저의 경우에는 붙여 넣기를 막고 있는데 이런 경우 allow pasting을 먼저 입력하고 엔터를 누른 후에야 복사한 내용을 붙여 넣을 수 있다.

application은 쿠키, 캐시, 이미지,폰트, 스타일시트 등 웹 에플리케이션과 관련된 리소스를 조회할 수 있다.
cookies에서는 브라우저에 저장이 된 쿠키 정보를 확인하고 수정할 수 있다.

또한 esc를 눌러 다른 패널과 콘솔 패널을 동시에 사용할 수도 있다.

페이지 소스 보기 -> 페이지와 관련된 소스 코드들을 모두 확인할 수 있다. -> ctrl + u

시크릿 모드에선 새로운 브라우저 세션이 생성이 되며 브라우저를 종료했을 때 방문 기록,
쿠키 및 사이트 데이터, 양식에 입력한 정보, 웹사이트에 부여된 권한과 같은 항목이 저장되지 않는다. -> 단축키 = ctrl + shift + n























